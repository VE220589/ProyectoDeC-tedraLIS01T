apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: tmnotes
type: Opaque
stringData:
  # En produccion real esta clave debe cambiarse y gestionarse como secreto.
  POSTGRES_PASSWORD: postgres
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: tmnotes
spec:
  # Almacenamiento persistente para conservar la base aunque el pod se reinicie.
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: tmnotes
spec:
  # La base de datos se mantiene con una sola replica para evitar conflictos de disco.
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: tmnotes
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_PASSWORD
          volumeMounts:
            # Datos reales de PostgreSQL.
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
            # Script SQL inicial creado como ConfigMap antes de aplicar este manifiesto.
            - name: postgres-init
              mountPath: /docker-entrypoint-initdb.d
      volumes:
        - name: postgres-data
          persistentVolumeClaim:
            claimName: postgres-data
        - name: postgres-init
          configMap:
            name: postgres-init
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: tmnotes
spec:
  # Servicio interno para que la app use DB_HOST=postgres.
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
